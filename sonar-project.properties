sonar.projectKey=katoutomohiro_juushin-care-system-v0-careapp8
sonar.organization=katoutomohiro

# Sources: src directory exists, so use it
sonar.sources=src,app,components,lib,hooks,services
sonar.tests=tests
sonar.exclusions=node_modules/**,**/*.spec.ts,**/*.spec.tsx,**/*.test.ts,**/*.test.tsx,**/at-case-record-form-offline.tsx,**/*-offline.tsx,**/*Offline.tsx
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.typescript.lcov.reportPaths=coverage/lcov.info

# Note: sonar.host.url is not needed for SonarCloud (only for SonarQube Server)
