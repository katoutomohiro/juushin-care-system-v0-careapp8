// Notification scheduling placeholder for future PWA/Notification API integration
// This module is intentionally minimal; wiring will be implemented in later steps.

export function scheduleReminder(time: string, message: string) {
  // Future: Notification API/PWA 連携用の予約ロジックをここに追加
  // 例: Service Worker 経由の通知、アラームマネージャ、またはバックグラウンド同期など
  // 現時点ではスタブ（何もしない）
  void time;
  void message;
}
