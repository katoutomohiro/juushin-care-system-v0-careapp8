"use client"

// Minimal stub component to avoid undefined element type during prerender.
// Push UI is disabled; this component renders nothing.

export function PushSubscriptionButton({ className }: { className?: string }) {
	return null
}
